CREATE TABLE product (
                           id int(11) NOT NULL AUTOINCREMENT,
                           name varchar(45) NOT NULL,
                           price float NOT NULL,
                           PRIMARY KEY (id)
);